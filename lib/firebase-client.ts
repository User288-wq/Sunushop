// lib/firebase-client.ts
// Version simplifiée pour éviter les erreurs d'initialisation

console.log("🔵 Firebase Client: mode simplifié");

// Exporter des objets vides pour éviter les erreurs
export const auth = null;
export const db = null;
export default null;
