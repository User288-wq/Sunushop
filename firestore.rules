rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🔓 MODE DÉVELOPPEMENT : Autoriser toutes les opérations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
